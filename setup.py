# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='error-correction-nn',
    version='0.0.1',
    description='',
    packages=find_packages(),
    requires=['chainer', ],
)
